var list = ["animales",]

function jaulasTerrestres (listAnimaes) {
	var listTerrestres = []
	for (var i = 0; i < listAnimaes.lenght; i++) {
		var listTerrestres = [i]
		if (animal.raza == 'Terrestre') {
			listTerrestres.push(animal)
		};
		if (animal.raza == 'Acuatico') {
			listAcuatico.push(animal)
		};
		if (animal.raza == 'Aereo') {
			listAereos.push(animal)
		};
		return listTerrestres

jaulasTerrestres(list)